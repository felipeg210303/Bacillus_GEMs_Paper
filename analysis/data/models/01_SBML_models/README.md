# 01_SBML_models/

The models from `../00_initial/` converted to SBML and renamed to BiGG nomenclature, so the same
reaction (say glucose uptake) carries the same id in every model.

Produced by `../../model_preparation/001_standardization.ipynb`. This is the input to the SBO
annotation step that yields `../02_final_annotated/`.
