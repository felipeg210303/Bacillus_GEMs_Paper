# 02_final_annotated/

The models after SBOannotator processing - **the tier every analysis notebook loads.**

One SBML file per model. `iBsu1103`, `iBsu1103v2` and `iBsu1209` are present but only used by
notebooks `01` and `02` (structural checks); from `03` onward the analyses run on the other seven.
