# analysis/data/

Inputs to the pipeline. Nothing here is generated by a notebook - generated output lives in
`../results/`.

- `models/` - the GEMs, in three tiers from raw download to annotated (see its README).
- `datasets/` - reference and experimental data the models are compared against.
